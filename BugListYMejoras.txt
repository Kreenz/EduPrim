general -> 
	- añadir ids de textos

login ->
	- quitar responsive del input

gestionActividades -> 
	- poner on delete cascade en las actividades
	- a la hora de borrar no carga bien la anterior actividad

listaUsuarios ->
	- El bloque izquierdo está demasiado pegado al borde de la pantalla, hay que meter más margen
	- Estaría bien que el apartado de filtros estuviera encima del buscador y más pequeño, no queda muy claro para que sirve en el sitio en el que está
	- el listado de filtros no tiene la misma font family que el resto de la página, lo mismo con el listado de usuarios(firefox)
	
login ->
	- colocar texto explicativo para el acceso de forma anónima mediante código a una actividad